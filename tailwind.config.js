theme: {
  extend: {
    colors: {
      primary: "var(--primary)"
      secondary: "var(--secondary)"
      accent: "var(--accent)"
      bg-app: "var(--background)"
      textcolor: "var(--text)"
      darkMode: "class"
        }
  }
}