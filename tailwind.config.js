theme: {
  extend: {
    colors: {
      primary: "var(--primary)"
      secondary: "var(--secondary)"
      accent: "var(--accent)"
      appbg: "var(--background)"
      textcolor: "var(--text)"
      darkMode: "class"
        }
  }
}